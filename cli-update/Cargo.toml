[package]
edition = "2021"
name = "cli-update"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crossterm = "0.25"
locking = {path = "../locking", features = ["std"]}
